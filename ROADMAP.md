# HELIX OS - Roadmap

## Current State (Complete)

- 3-pack system: `holo_projector` + `helix_os` + `helix_term`
- 128×72 holographic pixel display (576 text_display entities)
- 110-node Virtual Filesystem with real path resolution
- 33 shell commands with dynamic VFS queries
- Full compiled programming language (lexer → parser → compiler → stack VM)
- 3-stage CTF: SABLE Rogue AI Containment (RECON → CRYPTO → EXPLOIT)
- Dialog UI for all interaction
- Safe execution: bounded recursion, schedule-based VM yielding, depth guards

## Potential Future Work

- [ ] Multi-line output buffering (show multiple println results at once)
- [ ] Scrollable terminal history on the holo display
- [ ] Additional builtin functions (rand, abs, str_len, char_code)
- [ ] File write support (create user files in /tmp)
- [ ] More CTF stages (network forensics, binary patching)
- [ ] Multiplayer: per-player sessions with separate cwd/user state
- [ ] Performance: UUID-cached pixel writes instead of tag selectors
- [ ] Restore mini-games (Pong, Tic-Tac-Toe) on the holo display
