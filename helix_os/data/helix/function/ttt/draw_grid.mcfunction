# helix:ttt/draw_grid — render 2 vertical and 2 horizontal lines on the projector
# Vertical lines at x=42 and x=85, horizontal at y=24 and y=48
scoreboard players set helix _ttt_i 0
function helix:ttt/grid_v_loop
scoreboard players set helix _ttt_i 0
function helix:ttt/grid_h_loop
